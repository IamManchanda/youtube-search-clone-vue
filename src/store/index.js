import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

// No State Management needed in this App, so Store is empty!
export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
});
